import { createStore } from 'vuex';
import authModule from './modules/auth/index.js';

export default createStore({
  modules: {
    authModule
  },
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  }
});
